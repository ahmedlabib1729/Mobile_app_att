<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Main Menu -->
    <menuitem
        id="menu_porter_root"
        name="Porter Express"
        parent="stock.menu_stock_root"
        sequence="50"
        groups="group_porter_user"/>

    <!-- Shipments Menu -->
    <menuitem
        id="menu_porter_shipments"
        name="Shipments"
        parent="menu_porter_root"
        action="action_porter_shipment"
        sequence="10"/>

    <!-- Configuration Menu -->
    <menuitem
        id="menu_porter_config"
        name="Configuration"
        parent="menu_porter_root"
        action="action_porter_config"
        sequence="100"
        groups="group_porter_manager"/>
</odoo>