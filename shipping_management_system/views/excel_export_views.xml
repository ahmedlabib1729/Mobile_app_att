<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Server Action للـ Export -->
    <record id="action_server_export_shipments" model="ir.actions.server">
        <field name="name">Export to Excel</field>
        <field name="model_id" ref="model_shipment_order"/>
        <field name="binding_model_id" ref="model_shipment_order"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">
if records:
    action = records.action_export_to_excel()
        </field>
    </record>
</odoo>